import 'package:flutter/material.dart';

class ChatDrAuroraScreen extends StatelessWidget {
  const ChatDrAuroraScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Dr. Aurora')),
      body: Center(child: Text('Chat UI + diagnostics (stub)')),
    );
  }
}
